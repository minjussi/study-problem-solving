# Study Problem Solving

코딩 테스트 합격을 위한 알고리즘 문제 풀이 기록입니다.

## Goals

- [ ] AI.SW마에스트로 코딩 테스트 대비

## What I learned

| 플랫폼 |문제명 | 유형 | 핵심 팁 |
| :---:| :--- | :---: | :--- |
| 프로그래머스 | 주식 가격 | 큐 | 스택에 `시간의 흐름`을 넣기 |
| 프로그래머스 | | 힙 | `최대, 최소`를 이용해야하는 문제. 완전 이진 트리 형태 |
| 프로그래머스 | 디스크 컨트롤러 | 힙 | 우선순위는 `(소요시간, 데이터)` 튜플로 넣기. 큐가 비었을 땐 `time` 점프|
| 프로그래머스 | 가장 큰 수 | 정렬 | 문자열 정렬 시 자릿수 늘려서 비교 (`lambda x: x*3`) |
| 프로그래머스 | H-Index | 정렬 | 내림차순 정렬 후, `citations[i] < i+1` 역전되는 순간의 `i`가 정답|
| 프로그래머스 | 의상 | 해시 | 조합 문제를 풀 때는 +1(유령 요소) 하고 전부 곱한 다음 -1 (유령 요소만 조합된 경우) |
| 프로그래머스 | 베스트 앨범 | 해시 | 딕셔너리를 2개 만들어서 해결. <br>`sort`와 `sorted`의 차이점 기억, sorted(dict.items(), key=lambda x: (-x[0], x[1])). <br> `튜플 안에 있는 값은 접근해서 바꿀 수 없음`, 하나의 장르가 여러 번 등장할 수 있기 때문에 리스트와 튜플을 동시에 사용한 것. |
| 프로그래머스 | | 완전 탐색 | 데이터가 `10,000개 이하`이면 수학 공식을 이용하는 것보다 다 뒤지는 것이 나을 수도 .. |
| 프로그래머스 | 소수 찾기 | 완전 탐색 | 소수를 찾는 방법. permutations 사용 |
| 프로그래머스 | | DFS/BFS | 주어진 모든 노드를 탐색<br> `DFS(깊이 우선 탐색)` - 재귀 함수/스택을 써서 하나를 깊이 있게 파는 방식 (ex. 모든 경로 탐색, 경로마다 특징 저장)<br> `BFS(너비 우선 탐색)` - 큐를 활용해 주변부터 탐색하는 방식 (ex. 최단 거리, 최소 이동 횟수, 미로 찾기)|
| 프로그래머스 | 중요한 단어를 스포 방지 | 완전 탐색 |문자열 인덱싱을 잘할 것. set의 사용.|
| 프로그래머스 | 바이러스 파이프| DFS+BFS | 그래프를 만들어 낼 때의 팁: 노드의 개수 n이면 n+1개를 만들어서 인덱스 에러 방지. <br> |
| 프로그래머스| 조건에 부합하는 중고거래 상태 조회| SQL | CASE-WHEN을 활용하여 select문 구성하기 <br> `CASE` <br> WHEN 조건1 THEN x <br> WHEN 조건2 THEN y <br> ELSE z <br> `END AS (변수명)`|
| 프로그래머스 | | SQL| 서브쿼리의 활용<br> `1. select 절`: 단일 계산값(무조건 값이 1개만 필요할 때 ex. 총 평균 값 등) <br> `2. from 절`: 임시테이블 제작 (집계함수 2번 이상 사용하거나, 조건에 따라 변환 후 다시 그룹화가 필요할 때 등) <br> `3. where 절`: 조건으로 거를 때 (특정 조건에 맞는 행만 걸러내는 역할)|
| 프로그래머스 | 총 거래 금액 조회 | SQL | 실행 순서 무조건 기억! `from -> where -> group by -> having -> select -> order by`|
| 프로그래머스 | 대장균 크기에 따라 분류 | SQL (Window Function)| NTILE(나누고 싶은 개수) OVER (ORDER BY 컬럼명) |
| 프로그래머스 | 식품분류별 가장 비싼 식품| SQL (Window Function) | `RANK()`:  <br> `DENSE_RANK()`: <br> `ROW_NUMBER()`: |

## Environment
- Python 3
